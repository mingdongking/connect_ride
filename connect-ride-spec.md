# Connect: Ride — UI 컴포넌트 명세서

> **버전** v0.5 · **대상 파일** `connect-ride-prototype.html`  
> 클릭 반응, 화면 전환 효과, 데이터 입력 예외 처리 로직을 컴포넌트 단위로 정리합니다.

---

## 목차

1. [공통 컴포넌트](#1-공통-컴포넌트)
2. [탑승 플로우 화면](#2-탑승-플로우-화면)
3. [로봇 현황 화면](#3-로봇-현황-화면)
4. [이용 내역 화면](#4-이용-내역-화면)
5. [화면 전환 흐름 요약](#5-화면-전환-흐름-요약)

---

## 1. 공통 컴포넌트

### 1-1. AppHeader

| 항목 | 내용 |
|------|------|
| 위치 | 모든 화면 최상단 고정 |
| 구성 | 로고 아이콘 `C` + 타이틀 `Connect: Ride` + 햄버거 메뉴 버튼 |

**클릭 반응**
- 햄버거 버튼: 현재 프로토타입에서 별도 동작 없음 (비활성 상태, 추후 확장 예정)

**예외 처리**
- 없음 (표시 전용 컴포넌트)

---

### 1-2. BottomNav (하단 탭 바)

| 탭 ID | 레이블 | 이동 대상 |
|-------|--------|----------|
| `ride` | 탑승 | 탑승 플로우 첫 화면 (출발지 선택) |
| `live-map` | 로봇 현황 | 실시간 로봇 현황 화면 |
| `history` | 내역 | 이용 내역 화면 |

**클릭 반응**
- 탭 전환 시 해당 탭 아이콘·텍스트가 `theme.core` 색상으로 강조됨
- `탑승` 탭 클릭 시: `step`을 `departure`로 초기화하여 출발지 선택 화면으로 이동

**화면 전환 효과**
- 전환 애니메이션 없음, 즉시 교체 (state 업데이트)

**예외 처리**
- 탑승 진행 중(로봇 이동 중 등) 다른 탭으로 이동해도 상태(`step`, `activeRobot` 등)는 보존됨
- `탑승` 탭 재클릭 시 강제로 `departure` 단계로 리셋됨 → 진행 중인 탑승이 있으면 상태가 초기화되는 점 주의 (실제 서비스에서는 확인 다이얼로그 필요)

---

### 1-3. RideBanner

| 항목 | 내용 |
|------|------|
| 위치 | 탑승 플로우 전 화면의 상단 |
| 표시 내용 | 🤖 아이콘 + "원하는 장소로 이동해보세요!" + 안내 문구 |

**클릭 반응**
- 없음 (표시 전용)

---

### 1-4. StepIndicator (단계 표시)

| 단계 | 표시 조건 | 시각 처리 |
|------|----------|----------|
| 1 출발 | 항상 | 현재 단계: 파란 배경 / 완료: `✓` 뱃지 |
| 2 도착 | 항상 | 출발지 코드 표시됨 |
| 3 호출 | 항상 | 도착지 코드 표시됨 |

**클릭 반응**
- 없음 (표시 전용, 직접 탭으로 단계 이동 불가)

---

### 1-5. MapFilters (탑승 가능 로봇 필터)

**클릭 반응**
- 버튼 클릭 시 `filters.rideable` 토글 (on/off)
- **ON 상태**: 버튼 배경 `theme.core`(진한 파란색), 텍스트 흰색
- **OFF 상태**: 배경 투명, 텍스트 muted

**데이터 로직**
- `robots.filter(r => r.status === 'idle').length`로 idle 로봇 수 카운트하여 뱃지에 표시
- 필터 ON 시 맵에서 idle 로봇 강조 표시

**예외 처리**
- idle 로봇이 0대여도 버튼 표시됨 (카운트 `0` 표시)

---

### 1-6. MapZoomControls (줌 버튼)

**클릭 반응**
- `+` / `−` 버튼 존재하나, 현재 프로토타입에서 실제 줌 동작 없음 (UI 목업 전용)

---

### 1-7. RouteChip (경로 칩)

| 칩 종류 | 색상 | 위치 |
|---------|------|------|
| 출발 칩 | `theme.core` (파란색) | 좌측 |
| 목적지 칩 | `theme.bright` (밝은 파란색) | 우측 |

**클릭 반응**
- `✕` 버튼 클릭 시:
  - 출발 칩 `✕` → 출발지·목적지 모두 초기화 + `departure` 단계로 복귀
  - 목적지 칩 `✕` → 목적지만 초기화 (출발지 유지)

---

### 1-8. PrimaryButton

| variant | 배경색 | 텍스트 | 사용처 |
|---------|--------|--------|--------|
| `primary` | `theme.core` | 흰색 | 주요 액션 |
| `ghost` | 투명 + 테두리 | `theme.core` | 보조 액션·취소 |
| `danger` | `#DC2626` | 흰색 | 위험 액션 (미사용) |

**클릭 반응**
- `onMouseDown` → `scale(0.98)` 눌림 효과
- `onMouseUp` / `onMouseLeave` → `scale(1)` 복귀
- `disabled` 상태: 배경 `theme.border`, 커서 `not-allowed`, 클릭 무반응

---

## 2. 탑승 플로우 화면

### 2-1. DepartureScreen (출발지 선택)

**진입 조건**: 최초 앱 진입 또는 `탑승` 탭 재클릭

**맵 인터랙션**

| 동작 | 결과 |
|------|------|
| 정류장 노드 탭 | 해당 노드를 출발지로 선택 → `ArrivalScreen`으로 즉시 전환 |
| 추천 노드(점선 원 애니메이션) | 현재 위치(`BAY-02`) 및 인근 BAY 노드 강조 표시 |
| 탑승 가능 로봇 필터 ON | idle 상태 로봇이 맵에서 `theme.core` 색상으로 강조됨 |

**화면 전환**
- 노드 탭 즉시 → `step: 'arrival'` 로 전환 (별도 애니메이션 없음)

**예외 처리**
- 노드를 선택하지 않으면 다음 단계 진행 불가 (호출 버튼 없음)
- 모든 노드가 선택 가능 (idle/in-use 구분 없이 탑승 가능)

---

### 2-2. ArrivalScreen (목적지 선택 + 호출)

**진입 조건**: 출발지 선택 완료 후 자동 진입

**맵 인터랙션**

| 동작 | 결과 |
|------|------|
| 목적지 노드 탭 | 목적지 선택, 목적지 칩 표시 + 호출 버튼 활성화 |
| 출발지 노드 탭 | 출발지·목적지 모두 초기화 → `DepartureScreen`으로 복귀 |
| 목적지 노드 재탭 | 목적지만 초기화 (출발지 유지) |

**호출 버튼 활성화 조건**

| 조건 | 버튼 상태 |
|------|----------|
| 목적지 미선택 | 비활성 (회색 안내 텍스트 "목적지를 선택해주세요") |
| 목적지 선택 완료 | 활성 "🤖 로봇을 호출할게요!" |

**화면 전환**
- "로봇을 호출할게요!" 클릭 → `DispatchingScreen`으로 즉시 전환

**예외 처리**
- 출발지와 동일한 노드를 목적지로 선택하려 해도 차단 로직 없음 (프로토타입 한계, 실서비스에서 추가 필요)
- 목적지 미선택 시 호출 버튼 렌더링 자체가 변경됨 (버튼 → 안내 텍스트)

---

### 2-3. DispatchingScreen (로봇 매칭 + 자동 출발)

**진입 조건**: "로봇을 호출할게요!" 버튼 클릭

**자동 진행 타이머**

| 시간 | 동작 |
|------|------|
| +0ms | phase 0: "주변 로봇 스캔 중" + 스피너 표시 |
| +1,300ms | phase 1: "최적 로봇 매칭 중" |
| +2,600ms | phase 2: idle 로봇 자동 매칭 완료, "매칭 완료 · 자동 출발" |
| +3,400ms | `EnRouteDepScreen`으로 자동 전환 (phase 2 후 800ms) |

**클릭 반응**

| 버튼 | 동작 |
|------|------|
| 취소 | 목적지 초기화 후 `ArrivalScreen`으로 복귀 |

**예외 처리**
- idle 로봇이 없는 경우: `robots[0]`(첫 번째 로봇)을 fallback으로 매칭
- 취소는 phase 0~1 중에도 가능 (phase 2 진입 후 800ms 내 취소하면 자동 전환이 클린업되지 않을 수 있음 — 프로토타입 한계)

---

### 2-4. EnRouteDepScreen (로봇 → 출발지 이동 중)

**진입 조건**: 매칭 완료 후 자동 전환

**자동 진행 로직**
- 1초마다 `routeProgress += 1/60` (총 60초 시뮬레이션)
- `routeProgress >= 1` 도달 후 600ms → `AtDepartureScreen` 자동 전환

**클릭 반응**

| 버튼 | 동작 |
|------|------|
| 호출 취소 | 출발지·목적지·로봇 모두 초기화 → `DepartureScreen` 복귀 |
| 스킵 → 도착 | `routeProgress = 1` 강제 설정 → 600ms 후 `AtDepartureScreen` 전환 |

**화면 전환 효과**
- 진행 바: `width` CSS transition `0.6s linear`로 부드럽게 증가
- 맵: 로봇 아이콘이 출발지 → 현재 위치로 실시간 이동 애니메이션

**예외 처리**
- `routeProgress`는 `Math.min(1, ...)` 처리로 1 초과 방지

---

### 2-5. AtDepartureScreen (출발지 탑승 안내)

**진입 조건**: 로봇이 출발지 도달 후 자동 전환

**표시 정보**
- 매칭된 로봇 ID, 출발지 코드
- 탑승 안내 2단계: ① 출발지 정류장 이동 ② 로봇 정지 확인 후 탑승

**클릭 반응**

| 버튼 | 동작 |
|------|------|
| 완료 · 탑승 완료 | `routeProgress: 0` 초기화 후 `EnRouteArrScreen`으로 전환 |

**예외 처리**
- 별도 입력값 없음, 버튼 항상 활성

---

### 2-6. EnRouteArrScreen (로봇 → 목적지 이동 중)

**진입 조건**: "탑승 완료" 버튼 클릭

**자동 진행 로직**
- 1초마다 `routeProgress += 1/90` (총 90초 시뮬레이션)
- `routeProgress >= 1` 도달 후 600ms → `AtArrivalScreen` 자동 전환

**클릭 반응**

| 버튼 | 동작 |
|------|------|
| 스킵 → 도착 | `routeProgress = 1` 강제 설정 → 600ms 후 `AtArrivalScreen` 전환 |

**화면 전환 효과**
- 진행 바: `0.6s linear` 트랜지션
- 맵: 로봇 아이콘이 출발지 → 목적지 방향으로 실시간 이동

**예외 처리**
- `routeProgress` 1 초과 방지 (`Math.min(1, ...)`)

---

### 2-7. AtArrivalScreen (목적지 도착 + 하차)

**진입 조건**: 로봇이 목적지 도달 후 자동 전환

**자동 진행 타이머**

| 시간 | 동작 |
|------|------|
| +0ms | phase 0: "로봇 정지 대기 중" + 스피너 |
| +3,000ms | phase 1: "하차가 완료됐어요!" + 헤더 배경색 `theme.core` → `theme.bright` 전환 |

**화면 전환 효과**
- 상단 배너 배경색 전환: `transition: background 0.6s ease`

**클릭 반응 (phase 1 이후에만 표시)**

| 버튼 | 동작 |
|------|------|
| 새 탑승 | 전체 상태 초기화 → `DepartureScreen` 복귀 |
| 완료 | 이용 내역에 `status: 'done'` 항목 추가 → `DepartureScreen` 복귀 |

**예외 처리**
- phase 0 동안에는 버튼 없음 (스피너만 표시), 사용자 강제 진행 불가
- `pushHistory` 미전달 시 내역 저장 로직 skip (`pushHistory && pushHistory(...)`)

---

## 3. 로봇 현황 화면

### 3-1. LiveMapScreen

**진입 조건**: 하단 탭 "로봇 현황" 클릭

**표시 정보**
- 전체 맵 (LIVE 뱃지 표시)
- 탑승 가능(idle) 대수 / 이용 중 대수 뱃지
- 로봇별 상태 카드 리스트: 로봇 ID, 상태(탑승 가능/이용 중), 위치 노드, 배터리

**상태별 시각 처리**

| 상태 | 카드 테두리 | 로봇 ID 배경 | 상태 도트 |
|------|-----------|-------------|---------|
| idle (탑승 가능) | `theme.core` 반투명 | `theme.core` | 초록 `#22C55E` |
| in-use (이용 중) | `theme.border` | `theme.muted` (회색) | 주황 `#F59E0B` |

**클릭 반응**
- 없음 (표시 전용, 개별 로봇 카드 탭 동작 없음)

**예외 처리**
- 로봇 데이터는 Tweaks 패널의 "로봇 대수" 슬라이더(2~6대)로 동적 변경 가능
- 로봇 0대 상태는 슬라이더 최솟값 2대로 방지됨

---

## 4. 이용 내역 화면

### 4-1. HistoryScreen

**진입 조건**: 하단 탭 "내역" 클릭

**필터 탭**

| 필터 ID | 레이블 | 매칭 조건 |
|---------|--------|---------|
| `all` | 전체 | 모든 항목 |
| `riding` | 사용 중 | `status === 'riding'` |
| `done` | 탑승 완료 | `status === 'done'` |
| `cancelled` | 호출 취소 | `status === 'cancelled'` |

**클릭 반응 — 필터 버튼**
- 클릭한 필터가 활성화됨 (배경 `theme.deep`, 텍스트 흰색)
- 카운트 뱃지는 필터 관계없이 전체 항목 기준으로 항상 표시

**클릭 반응 — 내역 카드**

| 카드 상태 | 탭 동작 | 시각 피드백 |
|----------|---------|-----------|
| `riding` (사용 중) | 해당 탑승의 `enroute-arr` 화면으로 복귀 (탑승 탭으로 이동) | `scale(0.98)` 눌림 효과 + 파란 배경 강조 |
| `done` (탑승 완료) | 반응 없음 | 기본 카드 스타일 |
| `cancelled` (호출 취소) | 반응 없음 | 기본 카드 스타일 |

**사용 중 카드 탭 시 상태 주입 로직**
```
mode: 'ride'
step: 'enroute-arr'
departure: h.from
arrival: h.to
activeRobot: h.robot
routeProgress: 0.4   // 40% 진행 지점에서 재개
```

**상태별 시각 처리**

| 상태 | 카드 배경 | 상태 뱃지 | 도트 색상 |
|------|---------|---------|---------|
| `riding` | `theme.core` (파란색) | 흰색 반투명 "사용 중 · 탭하여 확인" | 흰색 |
| `done` | `theme.card` (흰색) | `theme.sky` 배경 "탑승 완료" | `theme.core` |
| `cancelled` | `theme.card` (흰색) | `#FEE2E2` 배경 "호출 취소" | `#EF4444` |

**예외 처리**
- 해당 필터의 항목이 없으면 "해당 내역이 없어요" 안내 텍스트 표시
- `riding` 카드 탭 시 `routeProgress: 0.4`로 고정 진입 (실서비스에서는 실제 진행률 동기화 필요)
- 내역은 세션 내 `pushHistory`로만 추가됨, 새로고침 시 초기 시드 데이터로 리셋

---

## 5. 화면 전환 흐름 요약

```
[탑승 탭]
    │
    ▼
DepartureScreen ─── 노드 탭 ──────────────────────────────▶ ArrivalScreen
    ▲                                                            │
    │                                                    목적지 탭 + 호출 버튼
    │                                                            │
    │                                                            ▼
    │                                               DispatchingScreen
    │                                          (자동: 2.6s 매칭 → 0.8s 후 전환)
    │                                                            │
    │                                                            ▼
    │                                               EnRouteDepScreen
    │                                          (자동: 60s 이동 → 0.6s 후 전환)
    │                                                            │
    │                                                            ▼
    │                                               AtDepartureScreen
    │                                               (탑승 완료 버튼 클릭)
    │                                                            │
    │                                                            ▼
    │                                               EnRouteArrScreen
    │                                          (자동: 90s 이동 → 0.6s 후 전환)
    │                                                            │
    │                                                            ▼
    │                                               AtArrivalScreen
    │                                          (자동: 3s 후 하차 완료 표시)
    │                                                            │
    └──────── 새 탑승 / 완료 ◀──────────────────────────────────┘

[로봇 현황 탭] → LiveMapScreen (표시 전용)

[내역 탭] → HistoryScreen
               └─ riding 카드 탭 → 탑승 탭 enroute-arr 화면으로 복귀
```

---

*작성일: 2026-05-16 · Connect: Ride Prototype v0.5*
